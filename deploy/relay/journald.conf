# Installed to /etc/systemd/journald.conf.d/relay.conf. The relay logs to
# journald only; this caps disk use. Ubuntu's default SystemMaxUse is 10% of
# the filesystem (up to 4G).
[Journal]
SystemMaxUse=1G
SystemKeepFree=2G
MaxRetentionSec=14day
